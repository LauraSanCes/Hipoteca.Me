<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.consumirapi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity2"></activity>
        <activity android:name=".screen_catalogo.Screen_catalogo" />
        <activity android:name=".screen_amort.Item_fila" />
        <activity android:name=".screen_amort.MainActivity_AdpPrs" />
        <activity android:name=".screen_amort.Screen_amort" />
        <activity android:name=".screen_calculadora.Screen_calculadora" />
        <activity android:name=".screen_conver.Screen_conver" />
        <activity android:name=".screen_credits.Screen_credits" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>